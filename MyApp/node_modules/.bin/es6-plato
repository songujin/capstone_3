#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../es6-plato/bin/plato" "$@"
  ret=$?
else 
  node  "$basedir/../es6-plato/bin/plato" "$@"
  ret=$?
fi
exit $ret
